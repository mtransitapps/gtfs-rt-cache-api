# GTFS Real Time Cached API for MonTransit

Some GTFS Real Time APIs are limited to a number of calls / app over a small duration.

Will returned cached data for GTFS-RT Service Alerts, Trip Updates & Vehicle Positions.

Only enabled for those limite GTFS-RT APIs, else app will connect directly to unlimited APIs.